// useMemo and useCallback both are used for performance optimization in react

//use memo retains the result in cache
//usecallback stores the entire ftn in cache

function Memo(props){

    const [counterOne, setCounterOne] = useState(0);
    const [counterTwo, setCounterTwo ] = useState(0);
    return(
        <div>
            

        </div>
    )

}

